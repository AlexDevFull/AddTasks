<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AddTasks" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="textColorPrimary">@android:color/primary_text_light</item>
        <item name="buttonImage">@drawable/ic_garbage_button_light_theme</item>
    </style>

    <style name="Theme.AddTasks" parent="Base.Theme.AddTasks" />
</resources>